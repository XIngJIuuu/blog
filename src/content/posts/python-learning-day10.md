---
title: Python 学习笔记 Day 10：切片是浅拷贝，切片赋值会改原表
published: 2026-09-19
description: Python 第十天笔记，把「切片」这一个语法拆成两份清单：切片取值（`a[:]`）只是浅拷贝、内层仍与原表共享；切片赋值（`a[1:2] = [...]`）根本不造新对象，它在原对象上改内容、还能改长度。用八组实测例子分清 `x[:] = []` 与 `x = []` 的差别，配反转三写法（新表 / 迭代器 / 原地）、切片与索引的边界规则、字典版浅拷贝，以及力扣 344 那句 `-> None` 到底在强制什么。
tags: [Python, 学习笔记]
category: Python学习
slug: python-learning-day10
draft: false
pinned: false
---

> [!NOTE]
> 这是第十天 Python 学习笔记，形式和前几天不一样：**按语法条目排的可查清单**，例子全部本地跑过，每个结论下面都跟着实测输出。
>
> Day 7 第七节末尾埋过一句话——「`data[:]` 复制的也只是外壳」，当时只给了结论没展开。今天就是把这句话拆开：`[:]` 出现在等号**左边**和出现在右边，是两件完全不同的事。

## 一、先给三句话结论

```python
b = a[1:2]      # 取值：造一个新的外壳对象，里面装的是原对象的引用（浅拷贝）
a[1:2] = [8, 9] # 赋值：不造任何新对象，直接在 a 这个对象上改内容，长度还能变
a[1] = [8, 9]   # 单格赋值：也是原地改，但只能换一格，长度永远不变
```

判断只要看一件事：**`[` ] 里有没有冒号，以及它在等号的哪一边。**

| 写法 | 造新对象吗 | 长度能不能变 | 返回值 | 原来的引用者看得见吗 |
| :--- | :--- | :--- | :--- | :--- |
| `b = a[1:2]` | 造（外壳新的） | — | 新列表 | 内层共享，看得见 |
| `a[1:2] = [...]` | 不造 | **能** | `None` | 就是它自己，必然看得见 |
| `a[1] = x` | 不造 | 不能 | `None` | 同上 |
| `a = [...]` | 造，并且**换绑定** | — | — | **看不见**（`a` 已经不是原来那个对象） |

最后一行是今天最值钱的一条，第四节专门讲它。

## 二、切片取值：浅拷贝的完整证据

```python
a = [[1], [2], [3]]
b = a[:]
print(b[0] is a[0])   # True   ← 外壳是新的，槽里装的还是同一批对象

b[0].append(99)
print(a)              # [[1, 99], [2], [3]]   ← 改内层，原表跟着变
```

同样一动，换种写法结果就反过来：

```python
a2 = [[1], [2], [3]]
b2 = a2[:]
b2[0] = [99]          # 不是 append，是赋值
print(a2)             # [[1], [2], [3]]   ← 原表一点事没有
```

> [!TIP]
> 区别就在「动的是槽里那个对象」还是「动的是外壳的槽位」。
> `b[0].append(99)` 拿着槽里那个列表**就地改**，而所有浅拷贝的槽都还指着它，于是原表跟着变；
> `b2[0] = [99]` 只是把**新外壳的第 0 格**换指向另一个对象，原表那一格从头到尾没被碰过。
>
> 这条「容器可变、槽位可换」的区分在 Day 6 讲元组时出现过一次（元组的"不可变"只锁槽位，不锁槽里对象能不能改），今天是同一个道理的第二次落地。

## 三、切片赋值：原地改，还能动长度

```python
c = [1, 2, 3]
print(c, len(c), id(c))     # [1, 2, 3] 3 1747841838528

c[1:2] = [8, 9]
print(c, len(c), id(c))     # [1, 8, 9, 3] 4 1747841838528   ← id 一模一样
```

**长度从 3 变成 4，`id` 却没变**——说明这不是"造了一个新表再贴回去"，是列表对象自己被掏空又填满。切片赋值的语义是：**把等号左边那一整段删掉，把右边这个可迭代对象里的东西逐个塞进原位。** 右边塞几个，长度就变几个：

```python
c2 = [1, 2, 3]
c2[1:2] = []          # 右边是空的 → 那一格被删掉
print(c2)             # [1, 3]

c3 = [1, 2, 3]
c3[1:2] = [8, 9, 7, 5]
print(c3, len(c3))    # [1, 8, 9, 7, 5, 3] 6
```

和单格赋值对照着看，这是最容易记混的一对：

```python
c4 = [1, 2, 3]
c4[1] = [8, 9]        # 没有冒号 → 只换一格，塞进去的是「一个 list 元素」
print(c4, len(c4))     # [1, [8, 9], 3] 3   ← 长度不变，多了一层嵌套
```

| 写的是 | 语义 | `c` 变成 | 长度 |
| :--- | :--- | :--- | :--- |
| `c[1:2] = [8, 9]` | 把区间 `[1:2]` 换成两个元素 | `[1, 8, 9, 3]` | 4 |
| `c[1:2] = []` | 把区间删空 | `[1, 3]` | 2 |
| `c[1] = [8, 9]` | 把第 1 格换成一个列表对象 | `[1, [8, 9], 3]` | 3 |

## 四、`x[:] = []` 和 `x = []`：差三个字符，一个是清空、一个是换绑定

同一个列表，先给它贴一张别名贴纸，再分别清空：

```python
x = [1, 2, 3]
alias_x = x
x[:] = []             # 切片赋值：原地清空
print(x, alias_x)     # [] []      ← 两个都空了
print(id(x) == id(alias_x))   # True   ← 从头到尾是同一个对象
```

```python
y = [1, 2, 3]
alias_y = y
y = []                # 重新绑定：y 指向一个新对象
print(y, alias_y)     # [] [1, 2, 3]   ← 原来那个列表还满着
print(id(y) == id(alias_y))            # False
```

> [!TIP]
> 这一条就是 Day 6 那句「变量是贴在对象上的名字贴纸，赋值不复制数据」的反向用法：**赋值从来不改对象，赋值只改贴纸贴谁。**
> 想让**所有持有同一个引用的人**都看到"清空"，只有 `x[:] = []` 做得到；`x = []` 只是把自己脚下这根线挪开，别人手里那根还攥着老列表。
>
> 工程后果很直接：从一个缓存字典里 `lst = cache[key]` 拿到列表，想清空它就必须写 `lst[:] = []`。写 `lst = []` 的话，缓存里那个列表一个元素都没少，而 `lst` 已经是另一个对象了——这类 bug 单看函数内部完全正常。

## 五、反转的三种写法：一个返回新表、一个返回迭代器、一个原地改

```python
r = [1, 2, 3]
print(r[::-1])              # [3, 2, 1]   type = list                 ← 新列表
print(reversed(r))          # <list_reverseiterator object ...>       ← 迭代器对象
rv = reversed(r)
print(list(rv), list(rv))   # [3, 2, 1] []   ← 第二次是空的，而且不报错
print(r.reverse())          # None
print(r)                    # [3, 2, 1]   ← 原表被就地翻了
```

| 写法 | 返回 | 类型 | 改原表吗 | 能用几次 |
| :--- | :--- | :--- | :--- | :--- |
| `r[::-1]` | 新列表 | `list` | 不改 | 随便用 |
| `reversed(r)` | 反向迭代器 | `list_reverseiterator` | 不改 | **一次** |
| `r.reverse()` | `None` | `NoneType` | **原地改** | — |

第三列那个"第二次拿到空列表"是 Day 9 整篇的主线在这里直接落地了：`reversed()` 造的是迭代器，它不存数据、只存一个游标，游标走到头就回不来。要两份结果就当场 `list()` 转出来存着。

## 六、越界：切片不检查，索引检查

```python
e = [1, 2, 3]
print(e[1:10])      # [2, 3]            ← 右边超出去，自动截断
print(e[-100:2])    # [1, 2]            ← 左边超出去，自动贴到 0
print(e[10])        # IndexError: list index out of range
```

原因是两者的语义不同：**索引在承诺"给我那一格的东西"，格子上没有就必须报错；切片在承诺"给我一个由落在区间内的元素组成的新列表"，一个都没有就是空列表**——空列表是完全合法的答案，没有理由抛异常。

这条规则让两类写法特别好使：

```python
e[2:]        # [3]        不知道长度也能安全取尾巴
e[i:]        # i 超过长度时得到 []，不会炸
e[-100:2]    # 负下标越界自动夹到合法范围
```

代价是另一面：**越界的切片会静默给你空值，不报错。** `data[10:12]` 返回 `[]` 时，你既可能是"确实没数据"，也可能是下标算错了，两种情况长得一模一样。所以处理越界要自己写判断（`if i < len(data)`），别指望切片提示你。

## 七、字典版：浅拷贝在嵌套处咬人

```python
d = {'k': [1, 2]}
e2 = d.copy()
print(e2 is d)            # False   ← 外层字典是新的
print(e2['k'] is d['k'])  # True    ← 值里那个列表还是同一个

e2['k'].append(3)
print(d)                  # {'k': [1, 2, 3]}   ← 改到了原表

e2['new'] = 0
print(d)                  # {'k': [1, 2, 3]}   ← 加新键就改不到了
```

一内一外，正好对照：动**内层列表**双方都看得见，动**外层字典的键**只有 `e2` 自己看得见。

顺手把"哪些写法是浅的"列全（对嵌套列表实测）：

| 写法 | 造新外壳 | 内层共享 | 是深拷贝吗 |
| :--- | :--- | :--- | :--- |
| `b = a` | 不造 | 全共享 | 否，压根没拷 |
| `b = a[:]` | 造 | 共享 | 否 |
| `b = a.copy()` | 造 | 共享 | 否 |
| `b = list(a)` | 造 | 共享 | 否 |
| `b = copy.copy(a)` | 造 | 共享 | 否 |
| `b = copy.deepcopy(a)` | 造 | **不共享** | **是** |
| `b = a` 之外还有 `a += [x]` | 不造（原地 `extend`） | 共享 | 否 |

前四种浅拷贝写法实测行为完全一致（`b is a` 都是 `False`，`b[0] is a[0]` 都是 `True`），没有哪一种是"更安全的那个"。`deepcopy` 是唯一一个 `b[0] is a[0]` 也变成 `False` 的。

> [!TIP]
> 注意倒数那一行 `a += [x]`：它**不造新对象**，别名能看见（实测 `alias is x` 仍为 `True`，`alias` 也多了那个元素）。而 `a + [x]` 造新表、原表不动。
> 这和 Day 8 讲的 `+=` 在列表和元组上行为相反是同一条规则——`+=` 走的是原地那条路，只是元组没有原地可走，才退化成重新绑定。

## 八、力扣 344：`-> None` 是题目在明说"给我原地改"

题目要求把字符数组原地反转，签名写死了：

```python
def reverseString(self, s: List[str]) -> None: ...
```

`-> None` 不是客气话，是**判题依据**：判题系统不会看你返回什么（返回了也拿不到），它只看你传进来的那个列表对象被改了没有。

于是最容易写的两种"看起来对"的写法各有问题：

```python
s = s[::-1]           # 造了新列表，把局部名字 s 改贴到新对象上
                      # 传进来的那个列表纹丝不动 —— 判题必错
s[:] = s[::-1]        # 外层对象是同一个（实测 id 不变），确实原地了
                      # 但等号右边先新建了一份完整副本，额外空间 O(n)，不满足题目的 O(1)
```

两种写法的实测差别：

```python
s1 = ['a', 'b', 'c']
s1[:] = s1[::-1]
print(s1, id(s1))     # ['c', 'b', 'a'] 1747841843072   ← 和反转前同一个 id

s2 = ['a', 'b', 'c']
t = s2[::-1]
print(s2, t, s2 is t) # ['a', 'b', 'c'] ['c', 'b', 'a'] False   ← 原表没动
```

正解是双指针，一次只交换两个已有格子的指向，不新建任何列表：

```python
from typing import List


class Solution:
    def reverseString(self, s: List[str]) -> None:
        """Do not return anything, modify s in-place instead."""
        l, r = 0, len(s) - 1
        while l < r:
            s[l], s[r] = s[r], s[l]
            l += 1
            r -= 1


if __name__ == "__main__":
    a = ["h", "e", "l", "l", "o"]
    print(Solution().reverseString(a), a)
    # None ['o', 'l', 'l', 'e', 'h']   ← 返回 None，原数组被改对了
```

时间 O(n)，空间 O(1)（只多了 `l`、`r` 两个下标）。同一件事在 Day 7 那道「一维数组的动态和」上撞过：三个版本能不能过，差别全在"有没有动传进来那个对象"。

顺带把它的姐妹题也收了——力扣 217「存在重复元素」用集合一遍扫：

```python
class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        seen = set()
        for x in nums:
            if x in seen:
                return True
            seen.add(x)
        return False
```

O(n) 时间 / O(n) 空间，比双重循环的 O(n²) 少一层。`set` 能做去重靠的是哈希，而哈希的前提是元素**可哈希**：`set([[1], [2]])` 直接抛 `TypeError: unhashable type: 'list'`。列表不可哈希，正是因为可变元素进了表还能改，改完哈希值就变了，整张表作废——这就是 Day 6 说"只有不可变类型能当 key"时给的那条理由。

## 九、可带走的问题清单

合上文件，这几条能秒答才算过：

1. `b = a[:]` 之后 `b[0].append(9)`，`a` 变不变？`b[0] = 9` 呢？为什么两个结果不一样？
2. `c[1:2] = [8, 9]` 和 `c[1] = [8, 9]`，哪个会变长度？各自结果是什么？
3. 想让所有别名都看到列表被清空，写什么？`x = []` 为什么不行？
4. `r[::-1]` / `reversed(r)` / `r.reverse()` 各自返回什么类型、谁改了原表？
5. `e[10]` 抛异常，`e[1:10]` 不抛——为什么？越界切片静默给空值有什么坏处？
6. `{'k': [1, 2]}` 用 `.copy()` 拷一份，改内层列表和加新键，原字典分别看不看得见？
7. `a += [x]` 和 `a + [x]`，哪个原地？
8. 签名 `-> None` 在力扣 344 里到底约束了什么？`s = s[::-1]` 为什么一定错？

## 十、小结

切片这一个语法，横着看是两条互不相干的规则：**取值造新外壳（而且是浅的），赋值动原对象（还能动长度）。** 竖着看它又是那条老主线的又一次出现——名字和对象是两回事：

- 一根箭头（一个名字），重新绑定只是把箭头挪到别的对象上，原来那个对象一个字节都没少；
- 一次原地修改（`[:] = `、`.append()`、`+=`）才是真的在掏空或填满它，而所有指着它的名字一起受影响。

`x = []` 换的是箭头，`x[:] = []` 动的是箭头所指的那个东西。这一句能同时解释今天九个例子和力扣 344 的判题结果。
